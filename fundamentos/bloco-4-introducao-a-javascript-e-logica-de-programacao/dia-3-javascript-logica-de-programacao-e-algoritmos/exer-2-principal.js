// cirar uma variavel para ser o contador do for

let cont = 4;

// cirar um array para ser as linhas 

let lin = [];

// cirar um for para percorrer as colunas adicionando valore(s) na linha

for (index = 1; index <= cont; index ++) {
    lin.push('*')
    console.log(lin)
}
